<?php

/**
 * STUB — Campos del panel admin.
 *
 * Fase futura: helpers para pintar campos del formulario (texto, número,
 * select por whitelist, checkbox, repetidor de tarjetas) a partir del esquema
 * de cada bloque en config/blocks/{type}.php.
 *
 * @package OKIP
 */

if (! defined('ABSPATH')) {
    exit;
}
