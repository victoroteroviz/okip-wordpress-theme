<?php

/**
 * STUB — Panel admin (páginas de menú).
 *
 * Fase futura: registrar páginas en el back-office para editar, por página, el
 * orden y la `data` de cada instancia de bloque. Los cambios se guardarán en
 * wp_options (NUNCA en archivos del tema) y se inyectarán vía el filtro
 * `okip_page_blocks` (ver inc/data.php).
 *
 * Esta fase no implementa funcionalidad de admin.
 *
 * @package OKIP
 */

if (! defined('ABSPATH')) {
    exit;
}

// Pendiente: add_action('admin_menu', 'okip_register_admin_pages');
