<?php

/**
 * Placeholder de arquitectura — página SALA DE PRENSA.
 *
 * Aún no está diseñada. En fases futuras usará el bloque de noticias filtrado
 * por la categoría `sala-de-prensa` (posts nativos de WordPress).
 *
 * Lista vacía → fallback a the_content() en page.php.
 *
 * @package OKIP
 */

if (! defined('ABSPATH')) {
    exit;
}

return array(
    // Pendiente: ej. ['type'=>'news','instance_id'=>'press-news','data'=>['category'=>'sala-de-prensa']]
);
