<?php

/**
 * Placeholder de arquitectura — página CONTACTO.
 *
 * Esta página aún no está diseñada. Devuelve una lista vacía de bloques: el
 * motor (page.php) recurrirá entonces al contenido nativo (the_content) como
 * fallback. Cuando se diseñe, basta con declarar aquí sus instancias de bloque.
 *
 * @package OKIP
 */

if (! defined('ABSPATH')) {
    exit;
}

return array(
    // Pendiente de diseño. Ej.: bloque 'contact-form' cuando exista.
);
